scripts:
  before:
    - ".scripts/install-spacemacs.sh"
  after:
    - ".scripts/install-vim-vundle.sh"
files: [
  # emacs
  ["emacs/spacemacs.el", "${HOME}/.spacemacs.d/init.el"],

  # vim
  ["vim", "${HOME}/.vim"],
]

